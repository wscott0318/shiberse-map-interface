export const pixel = {
    x: 1,
    y: 1
};

export const range = {
    x: 188,
    y: 189,
};

export const stageProps = {
    height: window.innerHeight - 5,
    width: window.innerWidth,
    options: {
        backgroundAlpha: 0,
        antialias: true,
        resizeTo: window,
    }
}

export const zoomRange = {
    min: 5,
    max: 50,
}